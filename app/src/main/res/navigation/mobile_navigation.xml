<?xml version="1.0" encoding="utf-8"?>
<!--- Parte importante del nav component -->
<!--- Aqui se visualizan las diferentes pantallas(Fragmentos) que se gestionará en el Nav Drawer -->
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.av.act13.ui.home.HomeFragment"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_home" />
    <fragment
        android:id="@+id/nav_box"
        android:name="com.av.act13.CajaFragment"
        android:label="Productos en venta"
        tools:layout="@layout/fragment_caja" />
    <fragment
        android:id="@+id/nav_fav"
        android:name="com.av.act13.FavoritosFragment"
        android:label="Favoritos"
        tools:layout="@layout/fragment_favoritos" />
    <fragment
        android:id="@+id/nav_vender"
        android:name="com.av.act13.VenderFragment"
        android:label="Publicar producto a la venta"
        tools:layout="@layout/fragment_vender" />

</navigation>